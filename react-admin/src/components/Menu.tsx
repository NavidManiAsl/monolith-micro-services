import styled from "styled-components";

const Container = styled.section`
    width:300px;
    height:100%;
    border-right: 1px dotted gray;
    position:sticky;
    top:0;

`

const Menu = () => {
    return(
        <Container></Container>
    )
}

export default Menu;